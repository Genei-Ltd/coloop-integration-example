/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */

export interface paths {
  '/v1/organization/': {
    parameters: {
      query?: never
      header?: never
      path?: never
      cookie?: never
    }
    /**
     * Get organization
     * @deprecated
     * @description Get the organization selected by the current route scope.
     */
    get: operations['getOrganizationLegacy']
    put?: never
    post?: never
    delete?: never
    options?: never
    head?: never
    patch?: never
    trace?: never
  }
  '/v1/organization/members/': {
    parameters: {
      query?: never
      header?: never
      path?: never
      cookie?: never
    }
    /**
     * List organization members
     * @deprecated
     * @description List users in the scoped organization. Optionally filter by email.
     */
    get: operations['listOrganizationMembersLegacy']
    put?: never
    post?: never
    delete?: never
    options?: never
    head?: never
    patch?: never
    trace?: never
  }
  '/v1/projects/': {
    parameters: {
      query?: never
      header?: never
      path?: never
      cookie?: never
    }
    /**
     * List projects
     * @deprecated
     * @description List projects visible to the caller in the current organization scope.
     */
    get: operations['listProjectsLegacy']
    put?: never
    /**
     * Create project
     * @deprecated
     * @description Create a project in the current organization scope and initialize downstream project setup.
     */
    post: operations['createProjectLegacy']
    delete?: never
    options?: never
    head?: never
    patch?: never
    trace?: never
  }
  '/v1/projects/{projectId}': {
    parameters: {
      query?: never
      header?: never
      path?: never
      cookie?: never
    }
    /**
     * Get project
     * @deprecated
     * @description Get a project visible to the caller in the current organization scope.
     */
    get: operations['getProjectLegacy']
    put?: never
    post?: never
    delete?: never
    options?: never
    head?: never
    patch?: never
    trace?: never
  }
  '/v1/projects/{projectId}/memberships/': {
    parameters: {
      query?: never
      header?: never
      path?: never
      cookie?: never
    }
    /**
     * List project memberships
     * @deprecated
     * @description List project members visible to the caller in the current organization scope.
     */
    get: operations['listProjectMembershipsLegacy']
    put?: never
    /**
     * Create project membership
     * @deprecated
     * @description Grant a user access to a project in the current organization scope.
     */
    post: operations['createProjectMembershipLegacy']
    delete?: never
    options?: never
    head?: never
    patch?: never
    trace?: never
  }
  '/v1/organizations/': {
    parameters: {
      query?: never
      header?: never
      path?: never
      cookie?: never
    }
    /**
     * List organizations
     * @description List organizations accessible to the authenticated user. The response reflects the organizations the caller can access with their current authentication context.
     */
    get: operations['listOrganizations']
    put?: never
    post?: never
    delete?: never
    options?: never
    head?: never
    patch?: never
    trace?: never
  }
  '/v1/organizations/{organizationId}/': {
    parameters: {
      query?: never
      header?: never
      path?: never
      cookie?: never
    }
    /**
     * Get organization
     * @description Get the organization selected by the current route scope.
     */
    get: operations['getOrganization']
    put?: never
    post?: never
    delete?: never
    options?: never
    head?: never
    patch?: never
    trace?: never
  }
  '/v1/organizations/{organizationId}/members/': {
    parameters: {
      query?: never
      header?: never
      path?: never
      cookie?: never
    }
    /**
     * List organization members
     * @description List users in the scoped organization. Optionally filter by email.
     */
    get: operations['listOrganizationMembers']
    put?: never
    post?: never
    delete?: never
    options?: never
    head?: never
    patch?: never
    trace?: never
  }
  '/v1/organizations/{organizationId}/projects/': {
    parameters: {
      query?: never
      header?: never
      path?: never
      cookie?: never
    }
    /**
     * List projects
     * @description List projects visible to the caller in the current organization scope.
     */
    get: operations['listProjects']
    put?: never
    /**
     * Create project
     * @description Create a project in the current organization scope and initialize downstream project setup.
     */
    post: operations['createProject']
    delete?: never
    options?: never
    head?: never
    patch?: never
    trace?: never
  }
  '/v1/organizations/{organizationId}/projects/{projectId}': {
    parameters: {
      query?: never
      header?: never
      path?: never
      cookie?: never
    }
    /**
     * Get project
     * @description Get a project visible to the caller in the current organization scope.
     */
    get: operations['getProject']
    put?: never
    post?: never
    delete?: never
    options?: never
    head?: never
    patch?: never
    trace?: never
  }
  '/v1/organizations/{organizationId}/projects/{projectId}/memberships/': {
    parameters: {
      query?: never
      header?: never
      path?: never
      cookie?: never
    }
    /**
     * List project memberships
     * @description List project members visible to the caller in the current organization scope.
     */
    get: operations['listProjectMemberships']
    put?: never
    /**
     * Create project membership
     * @description Grant a user access to a project in the current organization scope.
     */
    post: operations['createProjectMembership']
    delete?: never
    options?: never
    head?: never
    patch?: never
    trace?: never
  }
  '/v1/organizations/{organizationId}/projects/{projectId}/transcripts/batch/create': {
    parameters: {
      query?: never
      header?: never
      path?: never
      cookie?: never
    }
    get?: never
    put?: never
    /**
     * Batch-import transcripts
     * @description Import one or more pre-transcribed transcripts into a project in a single call.
     *
     *     Use this endpoint when you already have speakers and turn-level transcripts from an external source (another ASR system, internal tooling, a chat-style interview) and want to load them into CoLoop without re-running transcription.
     *
     *     ## Transcript shapes
     *
     *     Each transcript is a flat object with a `type` field:
     *
     *     - `type: "audio"` / `type: "video"` — `media` is required. Each turn must supply `text`; `utterances` may also be supplied when word-level timing is available.
     *     - `type: "text"` — no `media`, no `utterances`. Each turn supplies `text` only. Use for chat-style interviews or screener questions.
     *
     *     ## Semantics
     *
     *     - **Partial success** — each transcript in the request is processed independently. The response returns a per-transcript result with status `created`, `skipped`, or `failed`. A failure on one transcript does not abort the batch.
     *     - **Idempotent via `externalId`** — every transcript must carry a caller-provided `externalId` that is unique within the project. Re-submitting the same `externalId` returns `skipped` with the existing `transcriptId` instead of creating a duplicate.
     *     - **Asynchronous media transfer** (audio/video only) — the recording at `media.downloadUrl` is fetched out-of-band after the response is returned. The URL must remain reachable for at least one hour after the request.
     *     - **Asynchronous processing** — once a transcript is created, downstream work (summary, Q&A extraction, concept labeling, indexing) runs in the background. The transcript appears in the project immediately but analysis fields populate over the following minutes.
     *     - **Batch size** — a single request can carry up to 100 transcripts.
     *
     *     ## Authorization
     *
     *     The caller must have edit access on the target project.
     *
     *     ## Current status
     *
     *     This endpoint currently runs in facade mode: idempotency is honored (re-submitting an `externalId` returns `skipped`), and a small share of submissions return realistic `failed` results so partners can exercise their retry logic. No real transcripts are created. Contact support@coloop.ai before integrating against production traffic.
     */
    post: operations['batchImportTranscripts']
    delete?: never
    options?: never
    head?: never
    patch?: never
    trace?: never
  }
}
export type webhooks = Record<string, never>
export interface components {
  schemas: never
  responses: never
  parameters: never
  requestBodies: never
  headers: never
  pathItems: never
}
export type $defs = Record<string, never>
export interface operations {
  getOrganizationLegacy: {
    parameters: {
      query?: never
      header?: never
      path?: never
      cookie?: never
    }
    requestBody?: never
    responses: {
      /** @description Default Response */
      200: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            data: {
              id: string
              name: string
              /** Format: date-time */
              createdAt: string
              /** Format: date-time */
              updatedAt: string
            }
          }
        }
      }
      /** @description Default Response */
      400: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      401: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      403: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      404: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      500: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
    }
  }
  listOrganizationMembersLegacy: {
    parameters: {
      query?: {
        email?: string
      }
      header?: never
      path?: never
      cookie?: never
    }
    requestBody?: never
    responses: {
      /** @description Default Response */
      200: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            items: {
              id: string
              /** Format: date-time */
              createdAt: string
              /** Format: date-time */
              updatedAt: string
              /** Format: email */
              email: string
            }[]
          }
        }
      }
      /** @description Default Response */
      400: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      401: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      403: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      404: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      500: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
    }
  }
  listProjectsLegacy: {
    parameters: {
      query?: never
      header?: never
      path?: never
      cookie?: never
    }
    requestBody?: never
    responses: {
      /** @description Default Response */
      200: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            items: {
              id: string
              name: string
              description: string
              /** Format: date-time */
              createdAt: string
              /** Format: date-time */
              updatedAt: string
              ownerId: string
              organizationId: string | null
            }[]
          }
        }
      }
      /** @description Default Response */
      400: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      401: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      403: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      404: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      500: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
    }
  }
  createProjectLegacy: {
    parameters: {
      query?: never
      header?: never
      path?: never
      cookie?: never
    }
    requestBody: {
      content: {
        'application/json': {
          name: string
          projectTags: string[]
          /** @enum {string|null} */
          storageRegion?: 'us' | 'uk' | 'eu' | null
          /** @description Project discoverability. If true, the project is confidential and only visible to project members. If false, the project is discoverable by all organization members. Defaults to false. */
          hidden?: boolean | null
          /** @description Member list visibility. If true, the member list is hidden from guests. If false, guests can see the member list. Defaults to false. */
          membersHiddenFromGuests?: boolean | null
        }
      }
    }
    responses: {
      /** @description Default Response */
      201: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            data: {
              id: string
              name: string
              description: string
              /** Format: date-time */
              createdAt: string
              /** Format: date-time */
              updatedAt: string
              ownerId: string
              organizationId: string | null
            }
          }
        }
      }
      /** @description Default Response */
      400: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      401: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      403: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      404: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      500: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
    }
  }
  getProjectLegacy: {
    parameters: {
      query?: never
      header?: never
      path: {
        projectId: string
      }
      cookie?: never
    }
    requestBody?: never
    responses: {
      /** @description Default Response */
      200: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            data: {
              id: string
              name: string
              description: string
              /** Format: date-time */
              createdAt: string
              /** Format: date-time */
              updatedAt: string
              ownerId: string
              organizationId: string | null
            }
          }
        }
      }
      /** @description Default Response */
      400: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      401: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      403: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      404: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      500: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
    }
  }
  listProjectMembershipsLegacy: {
    parameters: {
      query?: never
      header?: never
      path: {
        projectId: string
      }
      cookie?: never
    }
    requestBody?: never
    responses: {
      /** @description Default Response */
      200: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            items: {
              projectId: string
              /** Format: date-time */
              createdAt: string
              userId: string
              role:
                | ('admin' | 'editor' | 'analyst' | 'viewer')
                | ('guest_editor' | 'guest_analyst' | 'guest_viewer')
                | 'client_analyst'
              user: {
                /** Format: email */
                email: string
                id: string
              }
            }[]
          }
        }
      }
      /** @description Default Response */
      400: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      401: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      403: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      404: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      500: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
    }
  }
  createProjectMembershipLegacy: {
    parameters: {
      query?: never
      header?: never
      path: {
        projectId: string
      }
      cookie?: never
    }
    requestBody: {
      content: {
        'application/json': {
          userId: string
          role:
            | ('admin' | 'editor' | 'analyst' | 'viewer')
            | ('guest_editor' | 'guest_analyst' | 'guest_viewer')
            | 'client_analyst'
        }
      }
    }
    responses: {
      /** @description Default Response */
      201: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            data: {
              projectId: string
              /** Format: date-time */
              createdAt: string
              userId: string
              role:
                | ('admin' | 'editor' | 'analyst' | 'viewer')
                | ('guest_editor' | 'guest_analyst' | 'guest_viewer')
                | 'client_analyst'
              user: {
                /** Format: email */
                email: string
                id: string
              }
            }
          }
        }
      }
      /** @description Default Response */
      400: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      401: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      403: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      404: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      500: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
    }
  }
  listOrganizations: {
    parameters: {
      query?: never
      header?: never
      path?: never
      cookie?: never
    }
    requestBody?: never
    responses: {
      /** @description Default Response */
      200: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            items: {
              id: string
              name: string
              /** Format: date-time */
              createdAt: string
              /** Format: date-time */
              updatedAt: string
              /** @enum {string} */
              role:
                | 'analyst'
                | 'basic_member'
                | 'elevated_member'
                | 'admin'
                | 'champion'
            }[]
          }
        }
      }
      /** @description Default Response */
      400: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      401: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      403: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      404: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      500: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
    }
  }
  getOrganization: {
    parameters: {
      query?: never
      header?: never
      path: {
        organizationId: string
      }
      cookie?: never
    }
    requestBody?: never
    responses: {
      /** @description Default Response */
      200: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            data: {
              id: string
              name: string
              /** Format: date-time */
              createdAt: string
              /** Format: date-time */
              updatedAt: string
            }
          }
        }
      }
      /** @description Default Response */
      400: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      401: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      403: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      404: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      500: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
    }
  }
  listOrganizationMembers: {
    parameters: {
      query?: {
        email?: string
      }
      header?: never
      path: {
        organizationId: string
      }
      cookie?: never
    }
    requestBody?: never
    responses: {
      /** @description Default Response */
      200: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            items: {
              id: string
              /** Format: date-time */
              createdAt: string
              /** Format: date-time */
              updatedAt: string
              /** Format: email */
              email: string
            }[]
          }
        }
      }
      /** @description Default Response */
      400: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      401: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      403: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      404: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      500: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
    }
  }
  listProjects: {
    parameters: {
      query?: never
      header?: never
      path: {
        organizationId: string
      }
      cookie?: never
    }
    requestBody?: never
    responses: {
      /** @description Default Response */
      200: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            items: {
              id: string
              name: string
              description: string
              /** Format: date-time */
              createdAt: string
              /** Format: date-time */
              updatedAt: string
              ownerId: string
              organizationId: string | null
            }[]
          }
        }
      }
      /** @description Default Response */
      400: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      401: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      403: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      404: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      500: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
    }
  }
  createProject: {
    parameters: {
      query?: never
      header?: never
      path: {
        organizationId: string
      }
      cookie?: never
    }
    requestBody: {
      content: {
        'application/json': {
          name: string
          projectTags: string[]
          /** @enum {string|null} */
          storageRegion?: 'us' | 'uk' | 'eu' | null
          /** @description Project discoverability. If true, the project is confidential and only visible to project members. If false, the project is discoverable by all organization members. Defaults to false. */
          hidden?: boolean | null
          /** @description Member list visibility. If true, the member list is hidden from guests. If false, guests can see the member list. Defaults to false. */
          membersHiddenFromGuests?: boolean | null
        }
      }
    }
    responses: {
      /** @description Default Response */
      201: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            data: {
              id: string
              name: string
              description: string
              /** Format: date-time */
              createdAt: string
              /** Format: date-time */
              updatedAt: string
              ownerId: string
              organizationId: string | null
            }
          }
        }
      }
      /** @description Default Response */
      400: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      401: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      403: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      404: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      500: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
    }
  }
  getProject: {
    parameters: {
      query?: never
      header?: never
      path: {
        organizationId: string
        projectId: string
      }
      cookie?: never
    }
    requestBody?: never
    responses: {
      /** @description Default Response */
      200: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            data: {
              id: string
              name: string
              description: string
              /** Format: date-time */
              createdAt: string
              /** Format: date-time */
              updatedAt: string
              ownerId: string
              organizationId: string | null
            }
          }
        }
      }
      /** @description Default Response */
      400: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      401: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      403: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      404: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      500: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
    }
  }
  listProjectMemberships: {
    parameters: {
      query?: never
      header?: never
      path: {
        organizationId: string
        projectId: string
      }
      cookie?: never
    }
    requestBody?: never
    responses: {
      /** @description Default Response */
      200: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            items: {
              projectId: string
              /** Format: date-time */
              createdAt: string
              userId: string
              role:
                | ('admin' | 'editor' | 'analyst' | 'viewer')
                | ('guest_editor' | 'guest_analyst' | 'guest_viewer')
                | 'client_analyst'
              user: {
                /** Format: email */
                email: string
                id: string
              }
            }[]
          }
        }
      }
      /** @description Default Response */
      400: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      401: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      403: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      404: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      500: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
    }
  }
  createProjectMembership: {
    parameters: {
      query?: never
      header?: never
      path: {
        organizationId: string
        projectId: string
      }
      cookie?: never
    }
    requestBody: {
      content: {
        'application/json': {
          userId: string
          role:
            | ('admin' | 'editor' | 'analyst' | 'viewer')
            | ('guest_editor' | 'guest_analyst' | 'guest_viewer')
            | 'client_analyst'
        }
      }
    }
    responses: {
      /** @description Default Response */
      201: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            data: {
              projectId: string
              /** Format: date-time */
              createdAt: string
              userId: string
              role:
                | ('admin' | 'editor' | 'analyst' | 'viewer')
                | ('guest_editor' | 'guest_analyst' | 'guest_viewer')
                | 'client_analyst'
              user: {
                /** Format: email */
                email: string
                id: string
              }
            }
          }
        }
      }
      /** @description Default Response */
      400: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      401: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      403: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      404: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      500: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
    }
  }
  batchImportTranscripts: {
    parameters: {
      query?: never
      header?: never
      path: {
        organizationId: string
        projectId: string
      }
      cookie?: never
    }
    requestBody: {
      content: {
        'application/json': {
          /** @description Transcripts to import. Each is processed independently. Maximum 100 per request. */
          transcripts: {
            /** @description Caller-supplied identifier, unique per project, used for idempotency. Re-submitting the same `externalId` returns a `skipped` result with the existing `transcriptId` instead of creating a duplicate. */
            externalId: string
            /** @description Human-readable name shown in CoLoop. */
            name: string
            /** @description BCP-47 language code of the transcript (e.g. `en`, `fr-CA`). Required; `und` (undetermined) is not accepted — specify the transcript language explicitly. */
            languageCode: string
            /**
             * @description What kind of transcript this is. `audio` / `video` require `media`; turns may optionally include `utterances` for timing. `text` has no media file; `media` must be omitted and `utterances` on any turn is rejected.
             * @enum {string}
             */
            type: 'audio' | 'video' | 'text'
            /** @description The audio or video recording associated with this transcript. Required when `type` is `audio` or `video`; must be omitted when `type` is `text`. */
            media?: {
              /**
               * Format: uri
               * @description URL CoLoop will download the media file from. Must remain accessible for at least one hour after the request.
               */
              downloadUrl: string
              /** @description File extension without the leading dot, e.g. `mp4` or `m4a`. */
              extension: string
              /** @description MIME type of the media file (e.g. `video/mp4`). If omitted, CoLoop resolves it with a `HEAD` request to `downloadUrl`. */
              mimeType?: string | null
              /** @description Optional HTTP headers to include when downloading the file (e.g. authorisation tokens for a private bucket). */
              downloadHeaders?: {
                [key: string]: string
              } | null
            }
            /** @description Speakers appearing in the transcript. Turns reference these by index. */
            speakers: (
              | {
                  /** @description Display name for the speaker. */
                  name: string
                  /**
                   * @description The respondent.
                   * @enum {string}
                   */
                  role: 'participant'
                  /**
                   * @description Segments the participant belongs to (e.g. demographic group, cohort, customer tier). Created or reused by value within the project.
                   * @default []
                   */
                  segments?: string[]
                }
              | {
                  /** @description Display name for the speaker. */
                  name: string
                  /**
                   * @description The interviewer or moderator, human or AI/bot.
                   * @enum {string}
                   */
                  role: 'moderator'
                }
            )[]
            /** @description Speech turns, in chronological order. */
            turns: {
              /** @description Index into the enclosing transcript's `speakers` array, identifying who is speaking. */
              speakerIndex: number
              /** @description Full text of this speech turn. Always required and treated as canonical, even when `utterances` is provided. */
              text: string
              /** @description Optional timed sub-units of this speech turn. Omit for turn-level or text-only transcripts. Only valid when the enclosing transcript has `type: audio | video`. When provided, the concatenation of `utterances[].text` must equal `text` exactly, character for character (whitespace included). */
              utterances?: {
                /** @description The utterance text as it appears in the enclosing turn's `text`. May be a single word, a phrase, or a full sentence depending on the granularity of the source transcript. */
                text: string
                /** @description Start time of this utterance, in milliseconds from the start of the media. */
                startMs: number
                /** @description End time of this utterance, in milliseconds from the start of the media. */
                endMs: number
                /** @description ASR confidence for this utterance, between 0 and 1. Defaults to `1` when omitted. */
                confidence?: number | null
              }[]
            }[]
          }[]
        }
      }
    }
    responses: {
      /** @description Default Response */
      200: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description One result per transcript in the request, in the same order. */
            results: (
              | {
                  /** @enum {string} */
                  status: 'created'
                  /** @description ID of the newly created transcript. */
                  transcriptId: string
                  /** @description The `externalId` from the request, echoed back for matching. */
                  externalId: string
                }
              | {
                  /** @enum {string} */
                  status: 'skipped'
                  /** @description ID of the existing transcript that matches `externalId`. */
                  transcriptId: string
                  /** @description The `externalId` from the request, echoed back for matching. */
                  externalId: string
                }
              | {
                  /** @enum {string} */
                  status: 'failed'
                  /** @description The `externalId` from the request, echoed back for matching. */
                  externalId: string
                  /** @description Structured error. Branch on `errorCode` for specific failure modes and on `retriable` for retry decisions. */
                  error: {
                    /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
                    errorCode: string
                    /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
                    statusCode: number
                    /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
                    retriable: boolean
                    /** @description Human-readable error message. Format is not stable, do not parse for logic. */
                    message: string
                    /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
                    correlationId?: string
                    /**
                     * Format: date-time
                     * @description ISO 8601 timestamp when the error was produced.
                     */
                    timestamp?: string
                  }
                }
            )[]
          }
        }
      }
      /** @description Default Response */
      400: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      401: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      403: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      404: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
      /** @description Default Response */
      500: {
        headers: {
          [name: string]: unknown
        }
        content: {
          'application/json': {
            /** @description Stable application-level error code for programmatic branching. See the published vocabulary for known values. */
            errorCode: string
            /** @description HTTP status code categorizing this error. Informational when returned inside a batch result. */
            statusCode: number
            /** @description Whether re-submitting the same payload unchanged might succeed. `true` for transient failures, `false` when the payload itself needs to change. */
            retriable: boolean
            /** @description Human-readable error message. Format is not stable, do not parse for logic. */
            message: string
            /** @description Identifier that ties this error to server-side logs. Include when contacting support. */
            correlationId?: string
            /**
             * Format: date-time
             * @description ISO 8601 timestamp when the error was produced.
             */
            timestamp?: string
          }
        }
      }
    }
  }
}
